<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Memorama_kit_kat" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="cards_background_tv" parent="Theme.Memorama_kit_kat">
        <item name="background">@drawable/bg_cards</item>
        <item name="android:layout_width">@dimen/card_width_tv</item>
        <item name="android:layout_height">@dimen/card_width_tv</item>
        <item name="android:layout_margin">5dp</item>
        <item name="android:scaleType">fitCenter</item>
    </style>
    <style name="cards_background_phone" parent="Theme.Memorama_kit_kat">
        <item name="android:layout_width">@dimen/card_width_phone</item>
        <item name="android:layout_height">@dimen/card_width_phone</item>
        <item name="android:layout_margin">5dp</item>
        <item name="android:scaleType">fitCenter</item>
    </style>
    <style name="selector_btn" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="colorButtonNormal">@color/btn_reset</item>
        <item name="android:textColor">@color/text_color</item>
        <item name="strokeColor">@color/text_color</item>
        <item name="background">@color/btn_bg</item>
    </style>
</resources>